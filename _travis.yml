# instructions for travis build and deploy

language: r
pandoc_version: 2.6
cache:
  packages: no

#before_install:
# install:

before_script:
#  - chmod +x ./_build.sh
  - chmod +x ./_deploy.sh

script:
  - Rscript -e "bookdown::render_book('index.Rmd', 'bookdown::gitbook')"
  - Rscript -e "bookdown::render_book('index.Rmd', 'bookdown::pdf_book')"
#  - ./_deploy.sh 
  
#after_success:
#after_failure:
#after_script:
#before_deploy:

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep_history: true
  local_dir: build
  on:
    branch: master
    
#after_deploy: