language: r
r: 3.6.1
pandoc_version: 2.6

#before_install:
# install:

before_script:
  - chmod +x ./_build.sh
  - chmod +x ./_deploy.sh

script:
  - Rscript -e "bookdown::render_book('index.Rmd', 'bookdown::gitbook')"

  
#after_success:
#after_failure:
#after_script:
#before_deploy:

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: gh-pages

#after_deploy: